#!/bin/sh
set -e

if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
	invoke-rc.d squid3 stop
else
	/etc/init.d/squid3 stop
fi
sleep 3

# restore the backup of squid configuration
cp -f /usr/share/doc/mix-premium-squidconf/conf/squid3.conf.orig /etc/squid3/squid.conf

exit 0
